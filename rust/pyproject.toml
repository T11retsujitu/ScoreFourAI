[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "score_four_rs"
version = "0.0.1"
description = "Score Four core engine (Rust/PyO3 extension)"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
